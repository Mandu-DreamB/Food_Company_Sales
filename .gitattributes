# 리눅스 서버에서 실행되는 파일은 항상 LF로 체크아웃한다.
# (CRLF가 섞이면 서버에서 `bad interpreter: /usr/bin/env bash^M` 오류가 난다)
*.sh              text eol=lf
*.template        text eol=lf
Dockerfile        text eol=lf
docker-compose*.yml text eol=lf
.env.example      text eol=lf
